var age = 28;
if (age > 30)
    age += 10;
else
    age -= 5;
var sales = 1000;
var course = 'TypeScript';
var is_published = true;
var level;
function hello(test) {
    console.log(test);
    return test;
}
var test = [1, 2];
var user = [1, "Harsh"];
;
var mySize;
// number in this example is setting what it the function return type would be
function return_example(test) {
    console.log(test);
    return 0;
}
function age_salary(age, salary, taxYear) {
    return age * salary;
}
age_salary(10, 2000);
var employee = { id: 1, name: "Harsh" };
var students = { id: 1 };
var student = { id: 1 };
var employee_test = { id: 1 };
function he(val) {
    return val;
}
